{
  "name": "codex-collab-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "plugins/codex-collab"
  ],
  "scripts": {
    "build": "npm run build --workspace @codex-collab/protocol && npm run build --workspace @codex-collab/dashboard && npm run build --workspace @codex-collab/relay && npm run build --workspace codex-collab",
    "dev": "npm run dev --workspace @codex-collab/relay",
    "dev:dashboard": "npm run dev --workspace @codex-collab/dashboard",
    "dev:mcp": "npm run dev --workspace codex-collab",
    "test": "npm run build --workspace @codex-collab/protocol && npm run test --workspace @codex-collab/dashboard && npm run test --workspace @codex-collab/relay && npm run test --workspace codex-collab",
    "test:workspace-flow": "node scripts/probe-workspace-flow.mjs",
    "typecheck": "npm run validate:architecture && npm run build --workspace @codex-collab/protocol && npm run typecheck --workspace @codex-collab/dashboard && npm run typecheck --workspace @codex-collab/relay && npm run typecheck --workspace codex-collab",
    "validate:architecture": "node scripts/check-architecture.mjs",
    "validate:plugin": "python C:/Users/21497/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py ./plugins/codex-collab"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "overrides": {
    "@modelcontextprotocol/sdk": {
      "@hono/node-server": "2.0.11"
    }
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
